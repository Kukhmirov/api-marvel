import spinner from '../../resources/icon/Spinner200px.gif'
const Spinner = () => {
    return <img src={spinner} alt="spinner" style={{margin: '0 auto', background: 'none', display: 'block'}}/>
}

export default Spinner;